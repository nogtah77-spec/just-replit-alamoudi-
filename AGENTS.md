# تعليمات وكلاء المشروع (Antigravity & AI Agent Instructions)

هذه الوثيقة هي **الدليل الهندسي الإلزامي** لأي وكيل ذكاء اصطناعي (AI Agent) أو مهندس برمجي يعمل على تطوير منصة **العمودي للتسويق العقاري (Alamoudi Luxury Estate Hub)**.

---

## 🏛️ مصدر الحقيقة والمعمارية النشطة (Single Source of Truth)

1. **الواجهة الأمامية والتطبيق النشط:**
   - يقع في المجلد: `artifacts/alamoudi`
   - البنية: `React 18` + `TypeScript` + `Vite` + `Tailwind CSS` + `Wouter Router`.
   - النشر والتشغيل: منشور سحابيًا على `Vercel Edge Network` ومرتبط بمستودع GitHub الرسمي: `https://github.com/nogtah77-spec/-`.
2. **قاعدة البيانات والخدمات السحابية والتخزين الهجين (Hybrid Cloud Storage):**
   - **قاعدة بيانات Supabase:** (المشروع النشط: `https://inuouzvmujxjjzfxinvk.supabase.co`) لقاعدة بيانات PostgreSQL، والتواجد اللحظي (Realtime Engine)، والمستخدمين، والإعدادات، وبيانات العقارات النصية وروابط الصور.
   - **سحابة الوسائط Cloudinary:** (Email: `alamoudi.real.estate55@gmail.com` / Cloud: `vis04evc` / Preset: `h2ft0erz`) لتخزين كافة صور وسائط العقارات، أغلفة المناطق، خلفيات الموقع، ومعرض التشطيبات عبر CDN عالمي فائق السرعة، بمجلدات منظمة جغرافياً حسب المنطقة وكود العقار (`alamoudi_properties/{region}/{code}`), لمنع استهلاك حصة قاعدة البيانات (Zero DB Egress).
3. **تطبيق الويب التقدمي (PWA) ومشغل الخدمة:**
   - مشغل الخدمة النشط هو الإصدار `Service Worker v15` الموجود في `artifacts/alamoudi/public/sw.js`.

---

## ⚠️ قواعد ومعايير معمارية صارمة (Critical Safety Rules)

### 1. عداد المشاهدات السحابي (Property Views Counter)
- **ممنوع منعًا باتًا** زيادة المشاهدات داخل دوال الرسم المباشرة أو بدون حراسة الجلسة.
- كل استدعاء لزيادة المشاهدات يجب أن يحمى بـ:
  1. مرجع محلي (`useRef`) داخل المكوّن لمنع الحساب المزدوج أثناء إعادة الرسم.
  2. حارس الجلسة (`sessionStorage.getItem('viewed_prop_' + id)`).
- المشاهدات تُحفظ وتُزامن سحابيًا في مخزن `__property_views_store__` في جدول `site_settings` ليظهر نفس الرقم الدقيق على جميع الأجهزة (الهاتف، اللابتوب، التابلت).

### 2. التواجد اللحظي (Supabase Realtime Presence)
- يتم عبر قناة `alm_live_presence`.
- **يُمنع احتساب المدير أو الموظفين كزوار:**
  - أي مستخدم مسجل دخوله كمسؤول أو موظف يتم عمل `untrack` له فورًا ولا يتم إرسال حالته كزائر.
  - الزوار غير المسجلين فقط هم من يتم تعقبهم.
  - إذا فتح المدير التطبيق من الهاتف ولا يوجد زوار آخرون، فإن العداد يجب أن يعرض `0` بشكل طبيعي وصحيح.

### 3. إدارة عناوين النوافذ والصفحات (Window Titles)
- يتم التحكم في عنوان المتصفح مركزيًا عبر المكون `RouteTitleManager` في `artifacts/alamoudi/src/App.tsx`.
- **ممنوع تكرار النصوص:**
  - الصفحة الرئيسية: `الرئيسية | العمودي للتسويق العقاري` فقط بدون تكرار.
  - صفحة العقار: يظهر كود العقار فقط (مثل `F32`) لسهولة التصفح والتبويبات.
  - لا تضع نصوصًا مكررة مثل "العمودي للتسويق العقاري - الرئيسية | العمودي للتسويق العقاري".

### 4. مخازن الإعدادات المعزولة (Isolated System Stores)
- لتجنب تضارب الأعمدة في قاعدة البيانات، تُحفظ الإعدادات والمشاهدات وإحصائيات الزوار وخلفيات النظام كمفاتيح JSON معزولة في جدول `site_settings`:
  - `__property_views_store__` — إجمالي المشاهدات لكل عقار.
  - `__visitor_stats_store__` — إحصائيات زوار الموقع والتفاعل.
  - `__site_settings_store__` — الإعدادات العامة للموقع.
  - `__home_background_store__` — صور الخلفيات المخصصة.

### 5. نظام إشعارات الويب (Web Push Notifications)
- يستخدم تشفير VAPID ومعيار Web Push.
- يتم تسجيل الاشتراك وتخزينه في جدول الإعدادات السحابي.
- معالجة أحداث `push` و `notificationclick` تتم عبر `public/sw.js` وتوجيه المستخدمين لمسار العقار المطلوب.

---


### 6. معيار الأرقام الإنجليزية القياسية (0-9)
- **ممنوع منعاً باتاً** استخدام الأرقام المشرقية (١، ٢، ٣) أو تنسيق `ar-EG` في أي شاشة أو بطاقة أو إحصائية.
- كافة الأرقام المالية، مدد السداد، الأقساط، عدادات الزوار، والنسب المئوية يجب أن تُنسق حصراً بالإنجليزية القياسية عبر `toLocaleString("en-US")`.
- التواريخ تُعرض بأرقام لاتينية وأسماء شهور عربية عبر `ar-SA-u-nu-latn`.

### 7. معمارية حاسبة التمويل وحفظ التصفير الدائم
- حاسبة التمويل تدعم مدد السداد الكسرية بأنصاف السنوات (`0.5`، `1.5`، `2.5`، `3.5` إلخ).
- حلقة جدول السداد تحسب الأشهر المتبقية في آخر سنة كسرية عبر `Math.ceil(loanYears)` وبدون فقدان أي قسط.
- التصفير يُحفظ بشكل دائم في `localStorage` تحت `alm_mortgage_calculator_state` لضمان بقاء الخانات فارغة حتى بعد إغلاق المتصفح.

## 🎨 معايير الهوية البصرية والتصميم (Luxury UI & RTL Standards)

- **الاتجاه واللغة:** اللغة العربية هي الأساس، مع اتجاه كامل من اليمين إلى اليسار (RTL).
- **نظام الألوان الملكي:**
  - اللون الكحلي الليلي الملكي: `#10202D`
  - اللون الرملي الدافئ الفاخر: `#A9927D` (Warm Sand Taupe)
  - العاجي الدافئ: `Warm Ivory` (`#F5F3EE`)
- **التصميم الزجاجي الفاخر:** استخدام `Glassmorphism` و `backdrop-filter` مع حدود رملية ناعمة وتباين بصري عالي.
- **التوافق التام مع الجوال:** التصميم يبدأ من الهاتف أولاً (`Mobile-First`) مع سهولة اللمس والتصفح.

---

## 🚀 دورة البناء والتحقق (Build & Verification Pipeline)

قبل إنهاء أي مهمة أو اعتماد كود جديد:
1. فحص الأنواع الصارم (TypeScript):
   ```bash
   pnpm --filter @workspace/alamoudi run typecheck
   ```
2. اختبار البناء للإنتاج:
   ```bash
   pnpm --filter @workspace/alamoudi run build
   ```
3. التحقق البصري والسلوكي على الشاشات المختلفة (ديسكتوب وجوال).
4. عدم تنفيذ عمليات `git push` على الفروع الرئيسية إلا بعد التأكد التام وطلب المستخدم.

---

## 🧠 الذاكرة الدائمة (Persistent Agent Memory)
- الفهرس المرجعي للقرارات السابقة هو: `.agents/memory/MEMORY.md`.
- اقرأ القرارات السابقة في ملفات الذاكرة قبل اقتراح أي تعديل جوهري.
- عند اتخاذ قرار معماري دائم جديد، أنشئ ملف ذاكرة جديد في `.agents/memory/` وأضفه إلى الفهرس.
